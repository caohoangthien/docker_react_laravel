# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - frontend-laravel

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://thien.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_875dd199bf294809a8ce1b7229cf1fcd }}
        password: ${{ secrets.AzureAppService_ContainerPassword_2300bbee4e9d48b6808f1c408274b44a }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: thien.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_875dd199bf294809a8ce1b7229cf1fcd }}/laravel:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'frontend-laravel'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_282d586b9a1044bbad44f2bfc226795b }}
        images: 'thien.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_875dd199bf294809a8ce1b7229cf1fcd }}/laravel:${{ github.sha }}'